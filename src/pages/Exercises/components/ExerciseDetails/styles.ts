import {
  Stack as BaseStack,
  Typography as BaseTypography,
  styled,
} from '@mui/material';

export const ModalTitle = styled(BaseTypography)`
  font-weight: 600;
  font-size: 16px;
  margin: 20px 0;
`;

export const Stack = styled(BaseStack)`
  padding: 20px;
`