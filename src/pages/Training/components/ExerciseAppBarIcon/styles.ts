import styled from '@emotion/styled';
import AddIcon from '@mui/icons-material/Add';
import { COLORS } from 'colors';

export const AddExerciseButton = styled(AddIcon)`
  background-color: ${COLORS.DARKER};
`;
